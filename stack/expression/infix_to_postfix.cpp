#include <isotream>
#include <string>
#include <stack>
#include <vector>
using namespace std;

vector<char> IndixToPostfix(const string s) {
  stack<char> st;

  for(int i = 0; i < s.length(); i++) {
    if()
  }
}

int main() {
  return 0;
}
